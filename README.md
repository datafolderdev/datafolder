# datafolder
datafolder is a highly efficient hierarchical virtual folder/file structured JSON database. It is implemented in pure native Node.js Javascript and has zero dependency, with full index and trigger features supported. The data can be organized into folders and sub folders, thus naturally partitioned.
